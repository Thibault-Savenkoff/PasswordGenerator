# Coding Assertions

## Before commit

No automated tooling is wired up. Manual check:

| Order | Action | Checks |
| ----- | ------ | ------ |
| 1 | `npm start` → open browser | App loads, password generates, copy works, no console errors |

## Before push

Same as before commit. No CI configured.
